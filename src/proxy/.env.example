# ============================================================================
# NGINX PROXY MANAGER - Storage Configuration
# ============================================================================
#
# Two storage profiles are available:
#
# 1. NAMED VOLUME (default) - Best for most users
#    - Docker manages storage automatically
#    - Data persists across container restarts
#    - Easy to backup with Docker commands
#    - Usage: docker compose -f docker-compose.yml up -d
#
# 2. BIND MOUNT - For advanced users / NAS / specific paths
#    - Store data in specific directories (local, NAS, cloud mount)
#    - Full control over storage location
#    - Usage: docker compose -f docker-compose.yml -f docker-compose.bind.yml up -d
#
# ============================================================================

# For BIND MOUNT profile, set the host paths here (relative or absolute):
# Examples:
#   HOST_NGINX_PROXY_MANAGER_DATA=./nginx-proxy-manager/data                    (relative to compose file)
#   HOST_NGINX_PROXY_MANAGER_DATA=/mnt/nas/nginx-proxy-manager/data             (absolute Linux path)
#   HOST_NGINX_PROXY_MANAGER_DATA=C:\data\nginx-proxy-manager                   (absolute Windows path)
#
# Leave commented to use the default NAMED VOLUME profile
# HOST_NGINX_PROXY_MANAGER_DATA=./nginx-proxy-manager/data
# HOST_NGINX_PROXY_MANAGER_LETSENCRYPT=./nginx-proxy-manager/letsencrypt

# ============================================================================
# NGINX PROXY MANAGER Admin Interface Credentials (optional)
# ============================================================================
# Initial admin email and password (used on first startup)
# Leave empty to use defaults (admin@example.com / changeme)
# ADMIN_USER=admin@example.com
# ADMIN_PASSWORD=changeme
