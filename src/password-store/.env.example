# ============================================================================
# VAULTWARDEN PASSWORD MANAGER - Storage Configuration
# ============================================================================
#
# Two storage profiles are available:
#
# 1. NAMED VOLUME (default) - Best for most users
#    - Docker manages storage automatically
#    - Data persists across container restarts
#    - Easy to backup with Docker commands
#    - Usage: docker compose -f docker-compose.yml up -d
#
# 2. BIND MOUNT - For advanced users / NAS / specific paths
#    - Store data in a specific directory (local, NAS, cloud mount)
#    - Full control over storage location
#    - Usage: docker compose -f docker-compose.yml -f docker-compose.bind.yml up -d
#
# ============================================================================

# For BIND MOUNT profile, set the host path here (relative or absolute):
# Examples:
#   HOST_VW_DATA=./vaultwarden/data              (relative to compose file)
#   HOST_VW_DATA=/mnt/nas/vaultwarden            (absolute Linux path)
#   HOST_VW_DATA=C:\data\vaultwarden             (absolute Windows path)
#
# Leave commented to use the default NAMED VOLUME profile
# HOST_VW_DATA=./vaultwarden/data

# ============================================================================
# VAULTWARDEN Domain Configuration
# ============================================================================
# The HTTPS domain where Vaultwarden is accessible
# This is used for correct redirect URIs and API calls from clients
VAULTWARDEN_DOMAIN=https://passwords.lan

# ============================================================================
# VAULTWARDEN Invitation Token (optional)
# ============================================================================
# Required to allow new user registrations via invitation
# Generate with: openssl rand -hex 32
# Leave empty to disable invitations
# INVITATION_ORG_NAME=
# INVITATION_TOKEN=
