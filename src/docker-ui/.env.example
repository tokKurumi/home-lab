# ============================================================================
# PORTAINER - Storage Configuration
# ============================================================================
#
# Two storage profiles are available:
#
# 1. NAMED VOLUME (default) - Best for most users
#    - Docker manages storage automatically
#    - Data persists across container restarts
#    - Easy to backup with Docker commands
#    - Usage: docker compose -f docker-compose.yml up -d
#
# 2. BIND MOUNT - For advanced users / NAS / specific paths
#    - Store data in a specific directory (local, NAS, cloud mount)
#    - Full control over storage location
#    - Usage: docker compose -f docker-compose.yml -f docker-compose.bind.yml up -d
#
# ============================================================================

# For BIND MOUNT profile, set the host path here (relative or absolute):
# Examples:
#   HOST_PORTAINER_DATA=./portainer/data              (relative to compose file)
#   HOST_PORTAINER_DATA=/mnt/nas/portainer/data       (absolute Linux path)
#   HOST_PORTAINER_DATA=C:\data\portainer             (absolute Windows path)
#
# Leave commented to use the default NAMED VOLUME profile
# HOST_PORTAINER_DATA=./portainer/data

# ============================================================================
# PORTAINER Configuration
# ============================================================================

# Timezone for Portainer (default: Europe/Moscow)
TZ=Europe/Moscow
