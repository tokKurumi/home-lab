# Watchtower Configuration
#
# Watchtower is a stateless service that monitors Docker containers
# and automatically updates their images. No volumes or bind-mounts needed.

# ============================================================================
# WATCHTOWER Update Settings
# ============================================================================

# Cleanup unused images after update
WATCHTOWER_CLEANUP=true

# Poll interval for checking updates (in seconds)
# 60 = check every minute (useful for testing)
# 86400 = check once per day (recommended for production)
WATCHTOWER_POLL_INTERVAL=60

# Include stopped containers in update checks
WATCHTOWER_INCLUDE_STOPPED=true

# Automatically restart stopped containers after update
WATCHTOWER_REVIVE_STOPPED=true

# ============================================================================
# WATCHTOWER Advanced Options (uncomment to enable)
# ============================================================================

# Enable debug logging
# WATCHTOWER_DEBUG=true

# Only update containers with specific label: com.centurylinklabs.watchtower.enable=true
# (already configured via --label-enable command)

# Notifications on update (email, slack, etc.)
# WATCHTOWER_NOTIFICATION_URL=

# Run on specific schedule (cron format, requires --schedule flag)
# WATCHTOWER_SCHEDULE=0 2 * * *
